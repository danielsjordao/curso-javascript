/*
var // Não vem sendo muito utilizado porque pode causar alguns Bugs. Ele é utilizado globalmente
let // Ele é gerado dentro de um escopo (muito utilizado)
const // Ele é gerado dentro de um escopo, porém podemos armazenar dados apenas 1x

Pense muito bem nos nomes da variáveis
*/

var cachorro = "Zoi";
console.log(cachorro);

let lanche = "X-Bacon";
console.log(lanche);

lanche = "X-Salada";
console.log(lanche)

const nome = "Daniel";
console.log(nome);

nome = "Daniel Jordão"
console.log(nome);


