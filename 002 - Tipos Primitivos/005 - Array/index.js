console.log([1,2,3,4,5,"teste", true]);
console.log(["Daniel", "Jordão"]);
console.log(["Daniel", "Jordão"].length); //length = contar a qtde de elementos

["Daniel", "Jordao"].forEach((res, index) => {
    console.log(res, index);
});

/*
forEach mostra a posição de cada item no array ex. Daniel 0 Jordao 1

*/