/*
    && (E)
    || (OU)
    ! (Negação)
*/

//console.log( 1 === 1 && 2 >= 1); // true
//console.log( 1 === 1 && 1 >= 2); // false pq analisa o conjunto de expressões

//console.log( 1 === 2 || 1 === 1); // true pq busca dentro da expressão o valor verdadeiro

console.log(!false); // espera que a expressão após a negação seja verdadeira ou falsa;